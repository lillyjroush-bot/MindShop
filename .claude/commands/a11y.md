---
description: Audit and fix WCAG AA compliance — semantic HTML, ARIA, keyboard, contrast, reduced-motion
---

Invoke the forge-skills:accessibility skill.

Establish WCAG AA as baseline. Name the assistive technologies tested (VoiceOver iOS/macOS, NVDA, TalkBack).
Audit semantic HTML — every interactive element is a native element (button, a href, input) or has a documented reason it isn't.
Review ARIA — sparingly, only when native semantics aren't enough. No role on an element that already has one.
Verify keyboard nav — tab order matches visual, focus-visible always, Esc closes overlays, focus returns to trigger.
Check color contrast — 4.5:1 body, 3:1 large, 3:1 non-text UI. Color is never the sole signal for state.
Verify `prefers-reduced-motion` respected. Run screen-reader checklist for every UI surface.

After writing: "Accessibility written to .forge/accessibility.md."
