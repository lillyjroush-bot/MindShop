---
description: Establish design tokens, component library, and visual standards — semantic tokens, all 6 states, dark mode parity
---

Invoke the forge-skills:design-system skill.

Define brand foundation (voice, accent, neutral scale, density).
Write the semantic token layer — components reference semantic tokens only, never raw hex. Dark mode is a remapping, not a rewrite.
Define scales — typography (6-7 sizes), spacing (4px/8px grid), radius, motion, breakpoints.
For every interactive primitive, define all 6 states: default, hover, active, focus-visible, disabled, loading. Plus error for inputs.
Build the gallery in light + dark. Write composition rules + anti-patterns.

After writing: "Design system written to .forge/design-system.md. Pair with /interaction and /a11y."
