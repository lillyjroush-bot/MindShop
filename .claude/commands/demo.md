---
description: Write demo script with scenes, wow moments, fallbacks, seed-data checklist, and dry-run plan
---

Invoke the forge-skills:demo-narrative skill.

Define audience + goal (the one decision you want them to make after watching) + time budget with 20% buffer.
Write the one key insight the demo must land — a single sentence that every scene serves.
Script 5-7 scenes, each: setup → tension → wow → resolution. Per scene name the seed function (cross-ref `.forge/seed-data.md`), the fallback (recorded GIF / screenshot / narrated story), and the time budget.
Prepare top 10 Q&A items the audience will ask.
Schedule the dry-run within 24h of the demo — every wow moment fired, every fallback rehearsed.

After writing: "Demo narrative written to .forge/demo-narrative.md. Run /seed to verify data, then dry-run."
